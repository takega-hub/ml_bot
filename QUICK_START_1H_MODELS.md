# Быстрый старт: Обучение моделей на 1h таймфрейме

## 🚀 Обучение моделей на 1h

### Вариант 1: Использование скрипта-обертки (рекомендуется)

```bash
# Обучение всех моделей на 1h без MTF
python train_1h_models.py --no-mtf

# Обучение всех моделей на 1h с MTF (4h, 1d)
python train_1h_models.py --mtf
```

### Вариант 2: Прямое использование основного скрипта

```bash
# Обучение на 1h без MTF
python retrain_ml_optimized.py --interval 60m --no-mtf

# Обучение на 1h с MTF
python retrain_ml_optimized.py --interval 60m --mtf

# Для конкретного символа
python retrain_ml_optimized.py --symbol BTCUSDT --interval 60m --no-mtf
```

## 🧪 Тестирование и сравнение

После обучения запустите сравнение всех моделей (15m и 1h):

```bash
python compare_ml_models.py --detailed-analysis
```

Скрипт автоматически:
- Найдет все модели (и 15m, и 1h)
- Протестирует их
- Сравнит результаты
- Покажет лучшие модели для каждого символа

## 📊 Что изменилось

### Имена файлов моделей

**15m модели:**
- `rf_BTCUSDT_15_15m.pkl`
- `rf_BTCUSDT_15_mtf.pkl`

**1h модели:**
- `rf_BTCUSDT_60_1h.pkl`
- `rf_BTCUSDT_60_mtf_1h.pkl`

### Параметры обучения для 1h

- `forward_periods`: 2 (вместо 5) = 2 часа вперед
- `max_hold_periods`: 24 (вместо 96) = 24 часа
- MTF таймфреймы: 4h и 1d (вместо 1h и 4h)

## 🎯 Следующие шаги

1. **Обучите модели на 1h** (см. команды выше)
2. **Протестируйте все модели** (`compare_ml_models.py`)
3. **Сравните результаты** 15m vs 1h
4. **Выберите лучшие модели** для продакшена

## 💡 Советы

- Начните с обучения без MTF (`--no-mtf`) - это быстрее
- После тестирования решите, нужны ли MTF модели
- Используйте `--detailed-analysis` для полного анализа
- Сравните количество сделок и качество сигналов между 15m и 1h

## 📝 Пример полного цикла

```bash
# 1. Обучить модели на 1h
python train_1h_models.py --no-mtf

# 2. Протестировать и сравнить
python compare_ml_models.py --detailed-analysis

# 3. Посмотреть результаты
# (файл: ml_models_comparison_*.csv)
```
