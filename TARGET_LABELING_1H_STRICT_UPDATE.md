# 📝 ОБНОВЛЕНИЕ: Строгие параметры target labeling для 1h моделей

## ✅ Внесенные изменения

### Файл: `retrain_ml_optimized.py`

**Проблема:** 
- Текущие параметры для 1h моделей дают **41-71% сигналов** (слишком много)
- Целевой диапазон: **15-25% сигналов**

**Решение:**
Ужесточены параметры target labeling для 1h моделей:

#### Старые параметры (1h):
```python
forward_periods = 2        # 2 часа
threshold_pct = 0.3        # 0.3%
min_profit_pct = 0.3       # 0.3%
min_risk_reward_ratio = 1.5  # 1.5:1
max_hold_periods = 24      # 24 часа
```

#### Новые параметры (1h):
```python
forward_periods = 6        # 6 часов (увеличено с 2)
threshold_pct = 0.8        # 0.8% (увеличено с 0.3%)
min_profit_pct = 0.8       # 0.8% (увеличено с 0.3%)
min_risk_reward_ratio = 2.5  # 2.5:1 (увеличено с 1.5:1)
max_hold_periods = 48      # 48 часов (увеличено с 24)
```

#### Параметры для 15m моделей (без изменений):
```python
forward_periods = 5        # 75 минут
threshold_pct = 0.3        # 0.3%
min_profit_pct = 0.3       # 0.3%
min_risk_reward_ratio = 1.5  # 1.5:1
max_hold_periods = 96      # 24 часа
```

## 🎯 Обоснование

1. **Увеличение forward_periods с 2 до 6 часов:**
   - Для 1h таймфрейма нужно больше времени для значимых движений
   - 6 часов дают больше контекста для предсказания

2. **Увеличение threshold_pct и min_profit_pct с 0.3% до 0.8%:**
   - Более строгие пороги отфильтруют шумовые сигналы
   - Оставят только значимые движения

3. **Увеличение min_risk_reward_ratio с 1.5 до 2.5:**
   - Более высокое соотношение риск/награда
   - Оставит только качественные сигналы

4. **Увеличение max_hold_periods с 24 до 48 часов:**
   - Больше времени для достижения цели
   - Соответствует более длинному forward_periods

## 📊 Ожидаемые результаты

После переобучения с новыми параметрами ожидается:
- **Снижение % сигналов** с 41-71% до **15-25%**
- **Улучшение качества сигналов** (меньше шума)
- **Улучшение метрик** (PnL%, Win Rate, Sharpe Ratio)
- **Более стабильные предсказания**

## 🚀 Следующие шаги

1. **Переобучить 1h модели с новыми параметрами:**
   ```bash
   python train_1h_models.py --mtf
   ```

2. **Протестировать переобученные модели:**
   ```bash
   python compare_ml_models.py --only-1h --detailed-analysis --days 14
   ```

3. **Проверить распределение сигналов:**
   - При обучении будет показано распределение классов
   - Ожидается: 15-25% LONG + SHORT, 75-85% HOLD

4. **Сравнить результаты:**
   ```bash
   python compare_15m_vs_1h.py
   ```

## ⚠️ Важно

- Новые параметры **значительно строже**, что может привести к:
  - Меньшему количеству сигналов
  - Но более высокому качеству
  - Возможно, потребуется дополнительная настройка

- Если сигналов будет слишком мало (< 10%), можно немного ослабить параметры:
  - `threshold_pct`: 0.8 → 0.7
  - `min_profit_pct`: 0.8 → 0.7
  - `min_risk_reward_ratio`: 2.5 → 2.2

## 📅 Дата изменений

2026-02-10
