# Анализ качества Target Labeling

## 📊 Результаты анализа

### Распределение классов

| Символ   | LONG % | SHORT % | HOLD % | Дисбаланс LONG/SHORT | LONG TP % | SHORT TP % |
|----------|--------|---------|--------|---------------------|-----------|------------|
| BTCUSDT  | 9.3%   | 10.2%   | 80.5%  | 1.10:1              | 99.8%     | 99.9%      |
| ETHUSDT  | 16.8%  | 17.8%   | 65.4%  | 1.06:1              | 99.9%     | 100.0%     |
| SOLUSDT  | 21.3%  | 21.7%   | 57.0%  | 1.02:1              | 99.9%     | 100.0%     |

## ✅ Положительные моменты

1. **Достижимость TP отличная** (99.8-100%)
   - Target labeling работает правильно
   - Почти все LONG/SHORT метки действительно достижимы

2. **LONG/SHORT хорошо сбалансированы** (1.02-1.10:1)
   - Нет перекоса в одну сторону
   - Модель может учиться на обоих направлениях

3. **SOLUSDT имеет лучшее распределение** (21.3% LONG, 21.7% SHORT)
   - Больше обучающих примеров
   - Меньше дисбаланс классов

## ⚠️ Проблемы

### 1. Сильный дисбаланс классов (HOLD доминирует)

**BTCUSDT**: 80.5% HOLD, только 9.3% LONG + 10.2% SHORT = **19.5% торговых сигналов**

**Проблема**: Модель будет предсказывать в основном HOLD, что объясняет "0 сделок" в бэктестах.

**Причины**:
- Слишком строгие критерии для LONG/SHORT (`threshold_pct=0.5%`, `min_profit_pct=0.5%`)
- `forward_periods=5` (75 минут) может быть слишком коротким для волатильных рынков
- `min_risk_reward_ratio=1.5` может быть слишком строгим

### 2. Разница между символами

- **BTCUSDT**: 19.5% сигналов (очень мало!)
- **ETHUSDT**: 34.6% сигналов (лучше)
- **SOLUSDT**: 43.0% сигналов (хорошо)

**Вывод**: BTCUSDT требует более мягких параметров или адаптивных параметров.

## 🎯 Рекомендации по улучшению

### Вариант 1: Адаптивные параметры по волатильности (РЕКОМЕНДУЕТСЯ)

**Идея**: Использовать разные `forward_periods` и `threshold_pct` в зависимости от текущей волатильности.

**Для низкой волатильности (BTCUSDT)**:
- `forward_periods=8` (2 часа вместо 75 минут)
- `threshold_pct=0.3%` (меньше порог)
- `min_profit_pct=0.3%` (меньше минимальная прибыль)

**Для высокой волатильности (SOLUSDT)**:
- `forward_periods=5` (75 минут - текущее)
- `threshold_pct=0.5%` (текущее)
- `min_profit_pct=0.5%` (текущее)

**Преимущества**:
- Больше сигналов для низковолатильных активов
- Сохранение качества для высоковолатильных
- Адаптация к рыночным условиям

### Вариант 2: Уменьшить строгость параметров (ПРОСТОЕ РЕШЕНИЕ)

**Для всех символов**:
- `threshold_pct=0.3%` (было 0.5%)
- `min_profit_pct=0.3%` (было 0.5%)
- `min_risk_reward_ratio=1.3` (было 1.5)
- `forward_periods=7` (105 минут вместо 75)

**Ожидаемый результат**: +30-50% больше LONG/SHORT сигналов

### Вариант 3: Символьно-специфичные параметры

**BTCUSDT** (низкая волатильность):
- `forward_periods=10` (2.5 часа)
- `threshold_pct=0.25%`
- `min_profit_pct=0.25%`

**ETHUSDT** (средняя волатильность):
- `forward_periods=7` (105 минут)
- `threshold_pct=0.4%`
- `min_profit_pct=0.4%`

**SOLUSDT** (высокая волатильность):
- `forward_periods=5` (75 минут) - текущее
- `threshold_pct=0.5%` - текущее
- `min_profit_pct=0.5%` - текущее

## 📈 План действий

### Шаг 1: Реализовать адаптивное target labeling

1. Вычислить волатильность (ATR или realized volatility)
2. Определить режим волатильности (низкая/средняя/высокая)
3. Применить соответствующие параметры

### Шаг 2: Переобучить модели с новыми параметрами

1. Запустить `retrain_ml_optimized.py` с адаптивными параметрами
2. Проверить распределение классов (цель: 30-40% LONG+SHORT для BTCUSDT)
3. Проверить достижимость TP (должна остаться >95%)

### Шаг 3: Сравнить результаты

1. Запустить бэктесты
2. Сравнить количество сделок (должно увеличиться)
3. Сравнить метрики (Win Rate, Profit Factor)

## ⚠️ Важные замечания

1. **Не жертвовать качеством ради количества**
   - Достижимость TP должна остаться >95%
   - Не уменьшать `min_risk_reward_ratio` слишком сильно (<1.2)

2. **Тестировать на разных символах**
   - BTCUSDT требует более мягких параметров
   - SOLUSDT может работать с текущими параметрами

3. **Мониторить переобучение**
   - Больше сигналов = больше данных для обучения
   - Но нужно следить за качеством на валидации

## 🔧 Текущие параметры target labeling

```python
forward_periods=5          # 75 минут
threshold_pct=0.5         # 0.5% порог
min_risk_reward_ratio=1.5 # 1.5:1
max_hold_periods=96       # 24 часа
min_profit_pct=0.5        # 0.5% минимальная прибыль
```

## 📝 Следующие шаги

1. ✅ Анализ target labeling - **ЗАВЕРШЕН**
2. ⏳ Реализовать адаптивное target labeling
3. ⏳ Переобучить модели
4. ⏳ Запустить бэктесты
5. ⏳ Сравнить результаты
