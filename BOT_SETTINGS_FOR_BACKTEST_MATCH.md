# Настройки бота для соответствия бэктесту

## 🎯 Цель
Настроить реальный бот так, чтобы он торговал **максимально идентично** бэктесту.

## 📊 Параметры бэктеста

### Размер позиции
- **Фиксированная сумма**: $100 (notional value)
- **Маржа**: $100 / leverage = $10 (при плече 10x)
- **Комиссия**: 0.0006 (0.06%) от суммы сделки

### TP/SL
- **SL**: Строго **1%** от цены входа (жестко зафиксирован)
- **TP**: Динамический, рассчитывается моделью (обычно 2-3% от цены входа)
- **R/R**: Обычно 2-3x (TP в 2-3 раза больше SL)

### Управление позициями
- **НЕТ DCA** (усреднения)
- **НЕТ Reverse** (реверса позиций)
- **НЕТ Trailing Stop** (трейлинг стоп)
- **НЕТ Partial Close** (частичного закрытия)
- **НЕТ Breakeven** (безубытка)
- **НЕТ Profit Protection** (защиты прибыли)
- **НЕТ Dynamic Position Sizing** (динамического изменения размера)

### Confidence Threshold
- **Минимальная уверенность**: 0.45 (45%)

---

## ⚙️ Что нужно изменить в настройках бота

### 1. Размер позиции

**Текущее значение в `config.py`:**
```python
base_order_usd: float = 50.0  # ❌ Неправильно
```

**Нужно изменить на:**
```python
base_order_usd: float = 100.0  # ✅ Правильно
```

**Где изменить:**
- В `bot/config.py` строка 82
- Или через переменную окружения: `BASE_ORDER_USD=100.0`
- Или через `risk_settings.json`:
```json
{
  "base_order_usd": 100.0
}
```

---

### 2. Отключить DCA (Усреднение)

**Текущее значение:**
```python
dca_enabled: bool = True  # ❌ Включено
```

**Нужно изменить на:**
```python
dca_enabled: bool = False  # ✅ Отключено
```

**Где изменить:**
- В `bot/config.py` строка 126
- Или через `risk_settings.json`:
```json
{
  "dca_enabled": false
}
```

**В UI бота:**
- Отключить переключатель "DCA"

---

### 3. Отключить Reverse (Реверс)

**Текущее значение:**
```python
reverse_on_strong_signal: bool = True  # ❌ Включено
```

**Нужно изменить на:**
```python
reverse_on_strong_signal: bool = False  # ✅ Отключено
```

**Где изменить:**
- В `bot/config.py` строка 132
- Или через `risk_settings.json`:
```json
{
  "reverse_on_strong_signal": false
}
```

**В UI бота:**
- Отключить переключатель "Reverse"

---

### 4. Отключить Trailing Stop (Трейлинг стоп)

**Текущее значение:**
```python
enable_trailing_stop: bool = True  # ❌ Включено
```

**Нужно изменить на:**
```python
enable_trailing_stop: bool = False  # ✅ Отключено
```

**Где изменить:**
- В `bot/config.py` строка 92
- Или через `risk_settings.json`:
```json
{
  "enable_trailing_stop": false
}
```

---

### 5. Отключить Partial Close (Частичное закрытие)

**Текущее значение:**
```python
enable_partial_close: bool = True  # ❌ Включено
```

**Нужно изменить на:**
```python
enable_partial_close: bool = False  # ✅ Отключено
```

**Где изменить:**
- В `bot/config.py` строка 97
- Или через `risk_settings.json`:
```json
{
  "enable_partial_close": false
}
```

---

### 6. Отключить Breakeven (Безубыток)

**Текущее значение:**
```python
enable_breakeven: bool = True  # ❌ Включено
```

**Нужно изменить на:**
```python
enable_breakeven: bool = False  # ✅ Отключено
```

**Где изменить:**
- В `bot/config.py` строка 113
- Или через `risk_settings.json`:
```json
{
  "enable_breakeven": false
}
```

---

### 7. Отключить Profit Protection (Защита прибыли)

**Текущее значение:**
```python
enable_profit_protection: bool = True  # ❌ Включено
```

**Нужно изменить на:**
```python
enable_profit_protection: bool = False  # ✅ Отключено
```

**Где изменить:**
- В `bot/config.py` строка 108
- Или через `risk_settings.json`:
```json
{
  "enable_profit_protection": false
}
```

---

### 8. Отключить Dynamic Position Sizing

**Текущее значение:**
```python
enable_dynamic_position_sizing: bool = True  # ❌ Включено
```

**Нужно изменить на:**
```python
enable_dynamic_position_sizing: bool = False  # ✅ Отключено
```

**Где изменить:**
- В `bot/config.py` строка 137
- Или через `risk_settings.json`:
```json
{
  "enable_dynamic_position_sizing": false
}
```

---

### 9. Проверить Confidence Threshold

**Текущее значение:**
```python
confidence_threshold: float = 0.45  # ✅ Правильно
```

**Проверить:**
- В `bot/config.py` строка 34
- Или через переменную окружения: `ML_CONFIDENCE_THRESHOLD=0.45`

---

### 10. Проверить SL и TP

**Текущие значения:**
```python
stop_loss_pct: float = 0.01  # ✅ Правильно (1%)
take_profit_pct: float = 0.02  # ⚠️ В бэктесте используется динамический TP от модели
```

**Важно:**
- SL должен быть строго **1%** (это уже правильно)
- TP в бэктесте рассчитывается динамически моделью (обычно 2-3%)
- В реальном боте TP также должен браться из сигнала модели, а не из `take_profit_pct`

---

## 📝 Пример `risk_settings.json`

Создайте или обновите файл `risk_settings.json` в корне проекта:

```json
{
  "base_order_usd": 100.0,
  "stop_loss_pct": 0.01,
  "take_profit_pct": 0.02,
  "fee_rate": 0.0006,
  "enable_trailing_stop": false,
  "enable_partial_close": false,
  "enable_breakeven": false,
  "enable_profit_protection": false,
  "dca_enabled": false,
  "reverse_on_strong_signal": false,
  "enable_dynamic_position_sizing": false,
  "long_term_ignore_reverse": true
}
```

---

## 🔍 Проверка соответствия

После изменения настроек проверьте:

1. **Размер позиции**: Должен быть $100 (notional value)
2. **SL**: Должен быть строго 1% от цены входа
3. **TP**: Должен браться из сигнала модели (динамический)
4. **Комиссия**: 0.06% от суммы сделки
5. **Нет DCA**: Позиции не усредняются
6. **Нет Reverse**: Позиции не реверсируются
7. **Нет Trailing Stop**: Стоп не двигается
8. **Нет Partial Close**: Позиция закрывается полностью
9. **Нет Breakeven**: Стоп не переводится в безубыток
10. **Нет Profit Protection**: Нет защиты прибыли

---

## ⚠️ Важные замечания

1. **TP берется из сигнала модели**: В бэктесте TP рассчитывается динамически моделью (обычно 2-3%). Убедитесь, что реальный бот использует `signal.take_profit` из сигнала, а не фиксированный `take_profit_pct` из конфига.

2. **SL строго 1%**: В бэктесте SL всегда строго 1% от цены входа. Убедитесь, что реальный бот не использует S/R уровни для SL, а всегда ставит 1%.

3. **Плечо**: В бэктесте используется плечо 10x. Убедитесь, что в реальном боте также установлено плечо 10x.

4. **Комиссия**: В бэктесте комиссия 0.0006 (0.06%). Убедитесь, что в реальном боте такая же комиссия.

---

## 🚀 Быстрая настройка через UI

Если у вас есть Telegram бот с UI, вы можете изменить настройки через команды:

1. Откройте настройки риска
2. Отключите:
   - DCA
   - Reverse
   - Trailing Stop
   - Partial Close
   - Breakeven
   - Profit Protection
3. Установите:
   - Amount: $100
   - SL: 1.00%
   - Min. Confidence: 45%

---

## 📋 Чеклист перед запуском

- [ ] `base_order_usd` = 100.0
- [ ] `dca_enabled` = false
- [ ] `reverse_on_strong_signal` = false
- [ ] `enable_trailing_stop` = false
- [ ] `enable_partial_close` = false
- [ ] `enable_breakeven` = false
- [ ] `enable_profit_protection` = false
- [ ] `enable_dynamic_position_sizing` = false
- [ ] `stop_loss_pct` = 0.01 (1%)
- [ ] `confidence_threshold` = 0.45 (45%)
- [ ] `fee_rate` = 0.0006 (0.06%)
- [ ] Плечо = 10x
- [ ] TP берется из сигнала модели (динамический)

---

## 🔄 После изменений

1. Перезапустите бота
2. Проверьте логи первых сделок
3. Сравните параметры сделок с бэктестом:
   - Размер позиции
   - SL (должен быть 1%)
   - TP (должен быть из модели, обычно 2-3%)
   - Комиссия
4. Убедитесь, что нет DCA, Reverse, Trailing Stop и других функций

---

## 📞 Если что-то не работает

Если после изменений бот ведет себя не так, как в бэктесте:

1. Проверьте логи бота на наличие ошибок
2. Убедитесь, что `risk_settings.json` загружается правильно
3. Проверьте, что все функции действительно отключены (нет вызовов DCA, Reverse и т.д.)
4. Сравните параметры открытых позиций с бэктестом
