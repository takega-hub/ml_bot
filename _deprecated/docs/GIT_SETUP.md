# 🔧 Настройка Git репозитория

## Первоначальная настройка

### 1. Инициализация Git (если еще не сделано)

```bash
git init
```

### 2. Добавление всех файлов

```bash
git add .
```

### 3. Первый коммит

```bash
git commit -m "Initial commit: ML Trading Bot"
```

### 4. Создание удаленного репозитория

#### На GitHub:

1. Перейдите на https://github.com
2. Создайте новый репозиторий (например, `ml_bot`)
3. **НЕ** инициализируйте его с README, .gitignore или лицензией
4. Скопируйте URL репозитория

#### На GitLab:

1. Перейдите на https://gitlab.com
2. Создайте новый проект
3. Скопируйте URL репозитория

### 5. Добавление remote и push

```bash
# Добавьте remote (замените URL на ваш)
git remote add origin https://github.com/yourusername/ml_bot.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на сервер
git push -u origin main
```

## Работа с Git

### Ежедневная работа

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на сервер
git push origin main
```

### Полезные команды

```bash
# Просмотр истории
git log --oneline

# Просмотр изменений
git diff

# Отмена изменений в файле
git checkout -- filename

# Создание новой ветки
git checkout -b feature-name

# Слияние веток
git merge feature-name
```

## Игнорируемые файлы

Файл `.gitignore` уже настроен и исключает:
- Виртуальное окружение (`venv/`)
- Файлы `.env` с секретами
- Логи (`*.log`, `logs/`)
- Кэш Python (`__pycache__/`)
- Временные файлы

⚠️ **Важно**: Убедитесь что `.env` файл НЕ попадает в репозиторий!

## Проверка что секреты не в репозитории

```bash
# Проверить что .env не отслеживается
git ls-files | grep .env

# Если .env в списке, удалите его из индекса
git rm --cached .env
git commit -m "Remove .env from tracking"
```

## Настройка SSH ключей (рекомендуется)

### Генерация SSH ключа:

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

### Добавление ключа на GitHub/GitLab:

1. Скопируйте публичный ключ:
```bash
cat ~/.ssh/id_ed25519.pub
```

2. Добавьте его в настройках GitHub/GitLab:
   - GitHub: Settings → SSH and GPG keys → New SSH key
   - GitLab: Preferences → SSH Keys

### Использование SSH URL:

```bash
git remote set-url origin git@github.com:yourusername/ml_bot.git
```

## Автоматизация деплоя

После настройки Git, на сервере можно настроить автоматический деплой:

1. Клонируйте репозиторий на сервер
2. Настройте webhook или cron для автоматического обновления
3. Используйте `deploy.sh` для автоматического деплоя

См. [DEPLOY.md](DEPLOY.md) для подробностей.
