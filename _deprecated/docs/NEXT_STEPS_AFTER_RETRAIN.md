# Следующие шаги после переобучения

## 📊 Текущие результаты

### ✅ Что работает хорошо

1. **Прибыльность моделей**: 56.7% моделей прибыльны
2. **Топ модели показывают отличные результаты**:
   - xgb_SOLUSDT_15_15m: +101.2%
   - xgb_ADAUSDT_15_15m: +87.0%
   - triple_ensemble_SOLUSDT_15_15m: +86.9%
3. **Качество сделок**: MFE/MAE = 2.60 (отлично)
4. **Баланс LONG/SHORT**: 1.02:1 (идеально)
5. **TP/SL**: 100% сигналов имеют TP/SL (в CSV)

### ⚠️ Проблемы, которые остались

1. **Использование сигналов**: 2.5% (цель: 30-40%) - КРИТИЧНО
2. **Конверсия сигналов в сделки**: 0.72% (цель: 10-15%) - КРИТИЧНО
3. **MTF модели**: Плохие результаты (0.91% vs 50.07% без MTF)

## 🔍 Анализ проблемы

### Почему использование сигналов низкое?

**Изменения в target labeling уже применены**:
- ✅ `threshold_pct`: 0.5% → 0.3%
- ✅ `min_profit_pct`: 0.5% → 0.3%
- ✅ `min_risk_reward_ratio`: 2.0 → 1.5

**Но использование сигналов не улучшилось** (осталось 2.5%)

**Вывод**: Проблема не в target labeling, а в **фильтрах стратегии** (`bot/ml/strategy_ml.py`), которые блокируют сигналы.

### Какие фильтры блокируют сигналы?

1. **`confidence_threshold`**: Базовый 0.45, но динамический может быть выше
2. **`stability_filter`**: Требует 1.1x уверенности для смены направления
3. **`min_signal_strength`**: Фильтр по силе сигнала
4. **RSI фильтры**: Блокируют сигналы в экстремальных зонах (>90/<10)
5. **Volume фильтры**: Блокируют сигналы при низком объеме (<0.3)
6. **`max_signals_per_day`**: Ограничивает до 10 сигналов в день
7. **Проверка BTCUSDT**: Может блокировать альткоины

## 🎯 План действий

### Шаг 1: Упростить фильтры стратегии (ПРИОРИТЕТ 1)

**Файл**: `bot/ml/strategy_ml.py`

**Изменения**:

1. **Снизить базовый `confidence_threshold`**:
   ```python
   # Текущее: 0.45
   # Рекомендуемое: 0.40
   confidence_threshold = 0.40
   ```

2. **Увеличить `max_signals_per_day`**:
   ```python
   # Текущее: 10
   # Рекомендуемое: 20-30
   max_signals_per_day = 20
   ```

3. **Ослабить `stability_filter`**:
   ```python
   # Текущее: 1.1x
   # Рекомендуемое: 1.05x или отключить для противоположных сигналов
   stability_threshold = max(confidence * 1.05, min_strength * 1.05)
   ```

4. **Ослабить RSI фильтры**:
   ```python
   # Текущее: >90/<10
   # Рекомендуемое: >95/<5 или отключить
   if rsi > 95 or rsi < 5:  # Было 90/10
       # Блокировать только в крайних случаях
   ```

5. **Ослабить volume фильтры**:
   ```python
   # Текущее: <0.3 при confidence > 0.8
   # Рекомендуемое: <0.2 при confidence > 0.85
   if confidence > 0.85 and volume_ratio < 0.2:  # Было 0.8 и 0.3
   ```

6. **Упростить проверку BTCUSDT**:
   - Возможно, сделать ее менее агрессивной
   - Или добавить опцию для отключения

### Шаг 2: Дополнительные изменения в target labeling (ПРИОРИТЕТ 2)

**Файл**: `retrain_ml_optimized.py`

**Изменения**:

1. **Уменьшить `forward_periods`**:
   ```python
   # Текущее: 5 (75 минут)
   # Рекомендуемое: 4 (60 минут)
   forward_periods = 4
   ```

2. **Увеличить `max_hold_periods`**:
   ```python
   # Текущее: 96 (24 часа)
   # Рекомендуемое: 120 (30 часов)
   max_hold_periods = 120
   ```

### Шаг 3: Применить оптимизированные веса (ПРИОРИТЕТ 3)

**Команда**:
```bash
python retrain_ml_optimized.py --use-optimized-weights --no-mtf
```

Это применит оптимизированные веса к ансамблям, что может улучшить результаты.

### Шаг 4: Добавить детальное логирование (ПРИОРИТЕТ 4)

**Цель**: Понять, какие именно фильтры блокируют сигналы

**Изменения в `bot/ml/strategy_ml.py`**:
- Добавить логирование причины отклонения каждого сигнала
- Сохранять статистику по типам отклонений

## 📋 Конкретные изменения в коде

### 1. `bot/ml/strategy_ml.py` - Упростить фильтры

```python
# Строка 52: Снизить базовый confidence_threshold
def __init__(self, model_path: str, confidence_threshold: float = 0.40, ...):  # Было 0.45

# Строка 96: Увеличить max_signals_per_day
self.max_signals_per_day = max_signals_per_day  # Изменить default в config.py на 20

# Строка 1033: Ослабить stability_filter
stability_threshold = max(confidence * 1.05, min_strength * 1.05)  # Было 1.1

# Строка 1055-1070: Ослабить RSI фильтры
if rsi > 95 or rsi < 5:  # Было 90/10
    # Блокировать только в крайних случаях

# Строка 1075-1085: Ослабить volume фильтры
if confidence > 0.85 and volume_ratio < 0.2:  # Было 0.8 и 0.3
    # Блокировать только при очень низком объеме и высокой уверенности
```

### 2. `bot/config.py` - Обновить defaults

```python
# Строка 37: Снизить confidence_threshold
confidence_threshold: float = 0.40  # Было 0.45

# Строка 47: Увеличить max_signals_per_day
max_signals_per_day: int = 20  # Было 10
```

### 3. `retrain_ml_optimized.py` - Дополнительные изменения

```python
# Строка 303: Уменьшить forward_periods
forward_periods=4,  # Было 5

# Строка 308: Увеличить max_hold_periods
max_hold_periods=120,  # Было 96
```

## 🧪 План тестирования

### После изменений:

1. **Переобучить модели** с новыми параметрами:
   ```bash
   python retrain_ml_optimized.py --no-mtf
   ```

2. **Протестировать модели**:
   ```bash
   python compare_ml_models.py --detailed-analysis
   ```

3. **Проверить улучшения**:
   - Использование сигналов должно увеличиться с 2.5% до 10-15%
   - Конверсия сигналов должна увеличиться с 0.72% до 5-10%
   - Прибыльность моделей должна остаться на уровне 50%+

## 📊 Ожидаемые результаты

### После упрощения фильтров:

- **Использование сигналов**: 2.5% → 10-15% (увеличение в 4-6 раз)
- **Конверсия сигналов**: 0.72% → 5-10% (увеличение в 7-14 раз)
- **Количество сделок**: Увеличение в 5-10 раз
- **Прибыльность**: Должна остаться на уровне 50%+ (возможно, немного снизится из-за большего количества сделок)

## ⚠️ Важные замечания

1. **Не жертвовать качеством ради количества**:
   - Win Rate должен остаться >50%
   - Profit Factor должен остаться >2.0

2. **Тестировать постепенно**:
   - Сначала упростить фильтры
   - Затем переобучить модели
   - Затем протестировать
   - Если результаты хорошие - применить оптимизированные веса

3. **Мониторить переобучение**:
   - Больше сигналов = больше данных для обучения
   - Но нужно следить за качеством на валидации

## 🎯 Приоритеты

1. **ПРИОРИТЕТ 1**: Упростить фильтры в `strategy_ml.py` (самое важное)
2. **ПРИОРИТЕТ 2**: Изменить параметры target labeling в `retrain_ml_optimized.py`
3. **ПРИОРИТЕТ 3**: Применить оптимизированные веса к ансамблям
4. **ПРИОРИТЕТ 4**: Добавить детальное логирование для диагностики
