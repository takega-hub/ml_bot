# Исправление проблемы с BTC фичами

## 🔍 Проблема

Модели были обучены **БЕЗ BTC фичей**, но в коде мы добавляем BTC фичи при генерации сигналов. Это создает **несоответствие**:
- Модель ожидает N фичей
- Мы даем N + 18 BTC фичей
- Модель получает неправильные данные → плохие предсказания

## ✅ Решение

Добавлена **автоматическая проверка совместимости**:
- Проверяем, есть ли BTC фичи в `feature_names` модели
- Если **НЕТ** - BTC фичи **НЕ добавляются** (избегаем несоответствия)
- Если **ДА** - BTC фичи добавляются нормально

## 📊 Как проверить

### 1. Проверить совместимость моделей:

```bash
# Проверить одну модель
python check_btc_features_compatibility.py --model ml_models/xgb_SOLUSDT_15_mtf.pkl

# Проверить все модели для символа
python check_btc_features_compatibility.py --symbol SOLUSDT
```

### 2. Что покажет:

```
✅ Модель обучена с BTC фичами: ДА/НЕТ
BTC фичей найдено: X/18
```

## ⚠️ Важно

### Если модель БЕЗ BTC фичей:

**Вариант 1: Переобучить модель** (рекомендуется)
```bash
python retrain_ml_optimized.py
```
- Модель будет обучена с BTC фичами
- BTC фичи будут работать корректно

**Вариант 2: Отключить BTC фичи** (временно)
- Закомментировать код в `strategy_ml.py`
- Модель будет работать как раньше

### Если модель С BTC фичами:

- Все работает нормально
- BTC фичи добавляются и используются

## 🎯 Текущее поведение

1. **Автоматическая проверка** при каждом вызове
2. **Предупреждение** если модель без BTC фичей
3. **Безопасная работа** - BTC фичи не добавляются если модель их не знает

## 📈 Ожидаемый результат

После исправления:
- Модели без BTC фичей работают как раньше (без BTC фичей)
- Модели с BTC фичами используют BTC фичи
- Нет ошибок несоответствия фичей
- Предсказания должны быть корректнее

## 🚀 Следующие шаги

1. ✅ Проверка совместимости добавлена
2. ⏳ Проверить текущие модели: `python check_btc_features_compatibility.py --symbol SOLUSDT`
3. ⏳ Если модели без BTC фичей - переобучить: `python retrain_ml_optimized.py`
4. ⏳ Протестировать снова
