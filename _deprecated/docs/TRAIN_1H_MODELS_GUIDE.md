# Руководство по обучению моделей на 1-часовом таймфрейме

## 📋 Обзор

Теперь вы можете обучать ML модели на **1-часовом таймфрейме (1h)** в дополнение к существующим **15-минутным (15m)** моделям. Это позволит:

1. **Диверсифицировать стратегии** - использовать разные таймфреймы для разных рыночных условий
2. **Сравнить эффективность** - понять, какой таймфрейм лучше работает для каждого символа
3. **Улучшить качество сигналов** - 1h модели могут быть более стабильными для долгосрочных трендов

## 🚀 Быстрый старт

### Обучение всех моделей на 1h (без MTF)

```bash
python train_1h_models.py --no-mtf
```

### Обучение всех моделей на 1h (с MTF - 4h, 1d)

```bash
python train_1h_models.py --mtf
```

### Обучение для конкретного символа

```bash
python train_1h_models.py --symbol BTCUSDT --no-mtf
```

## 📊 Использование напрямую через `retrain_ml_optimized.py`

Вы также можете использовать основной скрипт напрямую:

```bash
# Обучение на 1h без MTF
python retrain_ml_optimized.py --interval 60m --no-mtf

# Обучение на 1h с MTF
python retrain_ml_optimized.py --interval 60m --mtf

# Обучение для конкретного символа
python retrain_ml_optimized.py --symbol BTCUSDT --interval 60m --no-mtf
```

## 🔧 Параметры обучения

### Для 1h таймфрейма автоматически устанавливаются:

- **`forward_periods`**: 2 (вместо 5 для 15m) = 2 часа вперед
- **`max_hold_periods`**: 24 (вместо 96 для 15m) = 24 часа
- **MTF таймфреймы**: 4h и 1d (вместо 1h и 4h для 15m)

### Имена файлов моделей

Модели на 1h будут сохранены с именами:
- `rf_BTCUSDT_60_1h.pkl` (без MTF)
- `rf_BTCUSDT_60_mtf_1h.pkl` (с MTF)
- `xgb_BTCUSDT_60_1h.pkl`
- `ensemble_BTCUSDT_60_1h.pkl`
- и т.д.

## 🧪 Тестирование и сравнение

### Тестирование всех моделей (15m и 1h)

```bash
python compare_ml_models.py --detailed-analysis
```

Скрипт автоматически найдет все модели (и 15m, и 1h) и протестирует их.

### Сравнение результатов

После тестирования вы увидите:
- Лучшие модели для каждого символа (15m и 1h)
- Сравнение производительности
- Рекомендации по выбору таймфрейма

## 📈 Ожидаемые различия

### 15m модели:
- ✅ Больше сигналов (больше сделок)
- ✅ Быстрее реагируют на изменения
- ⚠️ Могут быть более шумными
- ⚠️ Больше ложных сигналов

### 1h модели:
- ✅ Более стабильные сигналы
- ✅ Меньше ложных сигналов
- ✅ Лучше для долгосрочных трендов
- ⚠️ Меньше сигналов (меньше сделок)
- ⚠️ Медленнее реагируют на изменения

## 🎯 Рекомендации

1. **Обучите модели на обоих таймфреймах** для всех символов
2. **Протестируйте и сравните** результаты
3. **Выберите лучший таймфрейм** для каждого символа на основе:
   - Profit Factor
   - Win Rate
   - Количество сделок
   - Максимальная просадка
4. **Используйте комбинацию** - можно использовать 15m для одних символов и 1h для других

## 🔍 Проверка обученных моделей

Модели сохраняются в директории `ml_models/`. Вы можете проверить:

```bash
# Список всех 1h моделей
ls ml_models/*_60_*.pkl

# Список всех 15m моделей
ls ml_models/*_15_*.pkl
```

## ⚙️ Дополнительные опции

### Использование оптимизированных весов

```bash
python train_1h_models.py --no-mtf --use-optimized-weights
```

Это применит оптимизированные веса ансамблей (если они были рассчитаны ранее).

## 📊 Примеры команд

```bash
# 1. Обучить все модели на 1h без MTF
python train_1h_models.py --no-mtf

# 2. Протестировать все модели (15m и 1h)
python compare_ml_models.py --detailed-analysis

# 3. Сравнить результаты
# (результаты будут в ml_models_comparison_*.csv)

# 4. Выбрать лучшие модели для продакшена
```

## 🐛 Устранение неполадок

### Ошибка: "Нет данных для символа"

Убедитесь, что:
- Символ корректен (например, BTCUSDT, а не BTC)
- API ключи настроены правильно
- Есть доступ к интернету

### Модели не находятся в `compare_ml_models.py`

Убедитесь, что:
- Модели сохранены в директории `ml_models/`
- Имена файлов соответствуют формату: `{type}_{symbol}_{interval}_{suffix}.pkl`
- Скрипт использует `--symbols auto` для автоматического поиска

## 📝 Примечания

- Обучение на 1h занимает меньше времени (меньше данных)
- 1h модели требуют меньше памяти
- Для 1h моделей можно использовать более длинные периоды обучения (больше дней данных)
- MTF для 1h использует 4h и 1d таймфреймы (вместо 1h и 4h для 15m)
