# Руководство по оптимизации и тестированию моделей

## ✅ Выполненные улучшения

### 1. Улучшены S/R фичи
**Файл**: `bot/ml/feature_engineering.py`

**Добавлено**:
- `support_touches` - количество касаний уровня поддержки
- `resistance_touches` - количество касаний уровня сопротивления
- `support_strength` - сила уровня поддержки (количество отскоков)
- `resistance_strength` - сила уровня сопротивления (количество отскоков)

**Использование**: Автоматически добавляются при создании фичей

### 2. Добавлены volatility regime фичи
**Файл**: `bot/ml/feature_engineering.py`

**Добавлено**:
- `atr_percentile_20`, `atr_percentile_50`, `atr_percentile_100` - ATR в перцентилях
- `volatility_regime` - режим волатильности (Low/Medium/High: 0/1/2)
- `volatility_trend` - тренд волатильности (растущая/падающая: 1/0)

**Использование**: Автоматически добавляются при создании фичей

### 3. Улучшена микроструктура
**Файл**: `bot/ml/feature_engineering.py`

**Добавлено**:
- `order_flow_imbalance` - дисбаланс потока ордеров (объем * направление)
- `high_volume_candles` - флаг свечей с высоким объемом
- `volume_intensity` - интенсивность объема относительно порога

**Использование**: Автоматически добавляются при создании фичей

## 🔧 Скрипты для оптимизации

### 1. Оптимизация гиперпараметров

**Файл**: `optimize_hyperparameters.py`

**Использование**:
```bash
# Оптимизация для всех 6 торговых пар (по умолчанию)
python optimize_hyperparameters.py --model rf --mtf

# Оптимизация для конкретного символа
python optimize_hyperparameters.py --model rf --symbol BTCUSDT --mtf

# Оптимизация для нескольких символов
python optimize_hyperparameters.py --model xgb --symbols BTCUSDT,ETHUSDT,SOLUSDT --mtf

# LightGBM для всех символов
python optimize_hyperparameters.py --model lgb --mtf
```

**Поддерживаемые символы** (по умолчанию все 6):
- SOLUSDT, BTCUSDT, ETHUSDT, XRPUSDT, ADAUSDT, BNBUSDT

**Что делает**:
- Собирает данные для указанного символа
- Создает фичи (с MTF, если указано)
- Проводит Grid Search с TimeSeriesSplit
- Оптимизирует гиперпараметры для максимизации accuracy
- Сохраняет лучшие параметры в JSON файл

**Результат**: JSON файл с оптимальными гиперпараметрами

**Пример вывода**:
```json
{
  "model": "rf",
  "symbol": "BTCUSDT",
  "best_params": {
    "max_depth": 12,
    "n_estimators": 150,
    "min_samples_split": 5
  },
  "best_score": 0.6234
}
```

### 2. Оптимизация весов ансамблей

**Файл**: `optimize_ensemble_weights.py`

**Использование**:
```bash
# Оптимизация для всех символов (автоматически находит модели)
python optimize_ensemble_weights.py \
    --models "rf_*_15_mtf.pkl,xgb_*_15_mtf.pkl,triple_ensemble_*_15_mtf.pkl" \
    --days 30

# Оптимизация для конкретного символа
python optimize_ensemble_weights.py \
    --symbol BTCUSDT \
    --models "ml_models/rf_BTCUSDT_15_mtf.pkl,ml_models/xgb_BTCUSDT_15_mtf.pkl,ml_models/triple_ensemble_BTCUSDT_15_mtf.pkl" \
    --days 30

# Оптимизация для нескольких символов
python optimize_ensemble_weights.py \
    --symbols BTCUSDT,ETHUSDT,SOLUSDT \
    --models "rf_*_15_mtf.pkl,xgb_*_15_mtf.pkl" \
    --days 30
```

**Поддерживаемые символы** (по умолчанию все 6):
- SOLUSDT, BTCUSDT, ETHUSDT, XRPUSDT, ADAUSDT, BNBUSDT

**Что делает**:
- Тестирует каждую модель на указанном периоде
- Вычисляет Sharpe ratio для каждой модели
- Оптимизирует веса для максимизации взвешенного Sharpe ratio
- Использует оптимизацию портфеля (Markowitz-style)

**Результат**: JSON файл с оптимальными весами

**Пример вывода**:
```json
{
  "symbol": "BTCUSDT",
  "weights": {
    "rf_BTCUSDT_15_mtf.pkl": 0.450,
    "xgb_BTCUSDT_15_mtf.pkl": 0.320,
    "triple_ensemble_BTCUSDT_15_mtf.pkl": 0.230
  }
}
```

### 3. Стресс-тестирование

**Файл**: `stress_test_models.py`

**Использование**:
```bash
# Стресс-тест всех RF моделей для всех символов
python stress_test_models.py \
    --model "rf_*_15_mtf.pkl"

# Стресс-тест конкретной модели
python stress_test_models.py \
    --model ml_models/rf_BTCUSDT_15_mtf.pkl \
    --symbol BTCUSDT

# Стресс-тест всех моделей определенного типа для всех символов
python stress_test_models.py \
    --model-type rf

# Стресс-тест для нескольких символов
python stress_test_models.py \
    --model-type triple_ensemble \
    --symbols BTCUSDT,ETHUSDT,SOLUSDT
```

**Поддерживаемые символы** (по умолчанию все 6):
- SOLUSDT, BTCUSDT, ETHUSDT, XRPUSDT, ADAUSDT, BNBUSDT

**Что делает**:
- Тестирует модель на разных периодах (14, 30, 60, 90 дней)
- Определяет рыночные условия (волатильность, тренд)
- Собирает метрики для каждого периода
- Анализирует стабильность результатов

**Результат**: JSON файл с результатами по всем периодам

**Пример вывода**:
```json
{
  "model": "rf_BTCUSDT_15_mtf.pkl",
  "results": [
    {
      "days": 14,
      "total_trades": 44,
      "win_rate": 63.6,
      "total_pnl_pct": 22.8,
      "volatility_regime": "medium",
      "trend_direction": "bullish"
    }
  ]
}
```

## 📊 Сравнение с предыдущими результатами

**Использование**:
```bash
python compare_ml_models.py \
    --compare-with ml_models_comparison_20260202_181257.csv \
    --detailed-analysis
```

**Что делает**:
- Сравнивает текущие результаты с предыдущими
- Показывает изменения PnL%, Win Rate, Profit Factor
- Определяет улучшенные модели
- Выводит статистику изменений

## 🚀 Рекомендуемый порядок действий

### 1. После добавления новых фичей

```bash
# 1. Переобучить модели с новыми фичами
python retrain_ml_optimized.py

# 2. Протестировать все модели
python compare_ml_models.py --detailed-analysis

# 3. Сравнить с предыдущими результатами
python compare_ml_models.py \
    --compare-with previous_results.csv \
    --detailed-analysis
```

### 2. Оптимизация лучших моделей

```bash
# 1. Оптимизировать гиперпараметры для RF для всех 6 символов
python optimize_hyperparameters.py --model rf --mtf

# 2. Применить оптимальные параметры в retrain_ml_optimized.py
# (вручную обновить параметры на основе JSON результатов для каждого символа)

# 3. Переобучить с оптимальными параметрами
python retrain_ml_optimized.py

# 4. Протестировать
python compare_ml_models.py --detailed-analysis
```

### 3. Оптимизация ансамблей

```bash
# 1. Оптимизировать веса для всех символов (автоматически находит модели)
python optimize_ensemble_weights.py \
    --models "rf_*_15_mtf.pkl,xgb_*_15_mtf.pkl,triple_ensemble_*_15_mtf.pkl" \
    --days 30

# 2. Применить оптимальные веса в model_trainer.py
# (вручную обновить веса на основе JSON результатов для каждого символа)

# 3. Переобучить ансамбли
python retrain_ml_optimized.py
```

### 4. Стресс-тестирование

```bash
# Тестировать все RF модели для всех символов
python stress_test_models.py --model-type rf

# Тестировать все TripleEnsemble модели
python stress_test_models.py --model-type triple_ensemble

# Тестировать конкретную модель
python stress_test_models.py --model ml_models/rf_BTCUSDT_15_mtf.pkl --symbol BTCUSDT

# Тестировать по паттерну
python stress_test_models.py --model "rf_*_15_mtf.pkl"
```

## 📈 Ожидаемые улучшения

### После добавления новых фичей:
- **S/R фичи**: Лучшее понимание силы уровней поддержки/сопротивления
- **Volatility regime**: Адаптация стратегии к режиму волатильности
- **Микроструктура**: Лучшее понимание потока ордеров

### После оптимизации гиперпараметров:
- **+5-10% Win Rate**: Более точные предсказания
- **+10-20% Profit Factor**: Лучший баланс прибыли/убытков
- **+0.5-1.0 Sharpe Ratio**: Лучшая доходность на единицу риска

### После оптимизации весов ансамблей:
- **+3-5% PnL%**: Более эффективное использование моделей
- **Лучшая стабильность**: Меньше просадок

## ⚠️ Важно

1. **Оптимизация гиперпараметров занимает время** (может быть 30-60 минут на модель)
2. **Используйте TimeSeriesSplit** для временных рядов (уже реализовано)
3. **Тестируйте на out-of-sample данных** после оптимизации
4. **Не переобучайте** - используйте разумные границы параметров
5. **Сохраняйте результаты** для сравнения

## 📝 Следующие шаги

1. ✅ Улучшены S/R фичи
2. ✅ Добавлены volatility regime фичи
3. ✅ Улучшена микроструктура
4. ✅ Созданы скрипты для оптимизации
5. ⏳ Переобучить модели с новыми фичами
6. ⏳ Оптимизировать гиперпараметры лучших моделей
7. ⏳ Оптимизировать веса ансамблей
8. ⏳ Провести стресс-тестирование
