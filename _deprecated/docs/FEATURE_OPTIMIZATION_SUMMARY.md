# Оптимизация фичей - выполненные изменения

## ✅ Удалены неэффективные фичи

На основе анализа feature importance (importance < 0.01) удалены:

### Паттерны свечей (6 фичей):
- ❌ `is_doji` (importance: 0.0001)
- ❌ `is_hammer` (importance: 0.0032)
- ❌ `is_shooting_star` (importance: 0.0062)
- ❌ `is_bullish_engulfing` (importance: 0.0)
- ❌ `is_bearish_engulfing` (importance: 0.0)
- ❌ `body_wick_ratio` (importance: 0.0054)

### Другие низкоэффективные фичи:
- ❌ `rsi_oversold` (importance: 0.0002)
- ❌ `near_bb_lower` (importance: 0.0001)
- ❌ `adx_trend_up` (importance: 0.0)

**Итого удалено**: ~9 фичей

## ✅ Добавлены усиленные MTF фичи

На основе анализа (MTF фичи имеют самую высокую importance):

### Новые MTF фичи:
- ✅ `rsi_overbought_60` - RSI overbought на 1h таймфрейме
- ✅ `rsi_overbought_240` - RSI overbought на 4h таймфрейме
- ✅ `macd_60` - MACD на 1h таймфрейме
- ✅ `macd_240` - MACD на 4h таймфрейме

**Итого добавлено**: 4 новые MTF фичи

## 📊 Ожидаемые улучшения

1. **Упрощение модели**: -9 фичей = меньше шума, быстрее обучение
2. **Улучшение точности**: +4 важные MTF фичи (особенно на 4h)
3. **Меньше overfitting**: удален шум от неэффективных фичей
4. **Лучшая интерпретируемость**: фокус на действительно важных фичах

## 🚀 Следующие шаги

1. **Переобучить модели** с новыми фичами:
   ```bash
   python retrain_ml_optimized.py
   ```

2. **Запустить бэктесты** для сравнения:
   ```bash
   python backtest_ml_strategy.py
   ```

3. **Сравнить результаты** с baseline (до оптимизации)

## 📝 Измененные файлы

- `bot/ml/feature_engineering.py`:
  - Удален блок создания паттернов свечей
  - Удалены `rsi_oversold`, `near_bb_lower`, `adx_trend_up`
  - Добавлены `rsi_overbought` и `macd` в список MTF фичей

## ⚠️ Важно

- Все изменения обратно совместимы (удаленные фичи просто не будут создаваться)
- MTF фичи (`rsi_overbought_60`, `rsi_overbought_240`, `macd_60`, `macd_240`) будут автоматически создаваться для всех MTF моделей
- Не-MTF модели не затронуты (они не используют MTF фичи)
