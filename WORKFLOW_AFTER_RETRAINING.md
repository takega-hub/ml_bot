# Последовательность действий после переобучения моделей

## 📋 Полная последовательность

### Шаг 1: Переобучение моделей ✅ (Вы делаете сейчас)

```bash
python retrain_ml_optimized.py
```

**Что происходит**:
- Обучаются модели для всех 6 символов (SOLUSDT, BTCUSDT, ETHUSDT, XRPUSDT, ADAUSDT, BNBUSDT)
- Создаются модели: RF, XGBoost, Ensemble, TripleEnsemble, QuadEnsemble
- Используются новые фичи (улучшенные S/R, volatility regime, микроструктура)
- Используются адаптивные параметры target labeling

**Результат**: Новые модели в `ml_models/`

---

### Шаг 2: Сравнение результатов (Базовый тест)

```bash
# Сохраняем результаты ДО оптимизации (baseline)
python compare_ml_models.py --detailed-analysis --output csv
```

**Что происходит**:
- Тестируются все новые модели на 30 днях (по умолчанию)
- Собираются метрики для всех моделей и символов
- Сохраняется CSV файл: `ml_models_comparison_YYYYMMDD_HHMMSS.csv`

**Результат**: CSV файл с результатами baseline

**Важно**: Запомните имя файла для сравнения!

---

### Шаг 3: Сравнение с предыдущими результатами (если есть)

```bash
# Если есть предыдущие результаты
python compare_ml_models.py \
    --compare-with ml_models_comparison_20260202_181257.csv \
    --detailed-analysis \
    --output csv
```

**Что происходит**:
- Сравнивает текущие результаты с предыдущими
- Показывает изменения PnL%, Win Rate, Profit Factor
- Определяет улучшенные модели

**Результат**: Обновленный CSV с колонками изменений

---

### Шаг 4: Оптимизация гиперпараметров (Опционально, но рекомендуется)

```bash
# Оптимизация для всех 6 символов (занимает 2-4 часа)
python optimize_hyperparameters.py --model rf --mtf
python optimize_hyperparameters.py --model xgb --mtf
python optimize_hyperparameters.py --model lgb --mtf
```

**Что происходит**:
- Grid Search для каждого символа отдельно
- Находит оптимальные гиперпараметры для максимизации accuracy
- Сохраняет результаты в JSON: `hyperparams_{model}_all_15_mtf_YYYYMMDD_HHMMSS.json`

**Результат**: JSON файлы с оптимальными параметрами для каждого символа

**Важно**: 
- Занимает много времени (2-4 часа на модель)
- Можно запустить на ночь
- После получения результатов нужно вручную обновить параметры в `retrain_ml_optimized.py`

---

### Шаг 5: Переобучение с оптимальными параметрами (Если делали шаг 4)

```bash
# 1. Обновить параметры в retrain_ml_optimized.py на основе JSON результатов
# 2. Переобучить
python retrain_ml_optimized.py

# 3. Снова протестировать
python compare_ml_models.py --detailed-analysis --output csv
```

**Что происходит**:
- Модели переобучаются с оптимальными гиперпараметрами
- Должны показать лучшие результаты

---

### Шаг 6: Оптимизация весов ансамблей (Опционально)

```bash
# Оптимизация весов для всех символов
python optimize_ensemble_weights.py \
    --models "rf_*_15_mtf.pkl,xgb_*_15_mtf.pkl,triple_ensemble_*_15_mtf.pkl" \
    --days 30
```

**Что происходит**:
- Тестирует каждую модель для каждого символа
- Оптимизирует веса на основе Sharpe ratio
- Сохраняет результаты: `ensemble_weights_all_YYYYMMDD_HHMMSS.json`

**Результат**: JSON файл с оптимальными весами для каждого символа

**Важно**: 
- После получения результатов нужно вручную обновить веса в `model_trainer.py`
- Затем переобучить ансамбли

---

### Шаг 7: Стресс-тестирование лучших моделей

```bash
# Стресс-тест всех RF моделей (лучшие результаты)
python stress_test_models.py --model-type rf

# Стресс-тест TripleEnsemble
python stress_test_models.py --model-type triple_ensemble

# Стресс-тест QuadEnsemble
python stress_test_models.py --model-type quad_ensemble
```

**Что происходит**:
- Тестирует модели на разных периодах (14, 30, 60, 90 дней)
- Определяет рыночные условия (волатильность, тренд)
- Анализирует стабильность результатов

**Результат**: JSON файл `stress_test_all_YYYYMMDD_HHMMSS.json`

---

### Шаг 8: Анализ результатов (Отправить мне)

**Что отправить**:
1. ✅ CSV файл сравнения: `ml_models_comparison_YYYYMMDD_HHMMSS.csv`
2. ✅ JSON файл стресс-теста: `stress_test_all_YYYYMMDD_HHMMSS.json` (если делали)
3. ✅ JSON файлы оптимизации (если делали):
   - `hyperparams_*_all_*.json`
   - `ensemble_weights_all_*.json`

**Что я проанализирую**:
- Сравнение с предыдущими результатами
- Лучшие модели по символам
- Стабильность результатов
- Рекомендации по дальнейшим улучшениям

---

## 🚀 Минимальная последовательность (быстрый вариант)

Если нет времени на оптимизацию:

```bash
# 1. Переобучение (вы делаете)
python retrain_ml_optimized.py

# 2. Тестирование и сравнение
python compare_ml_models.py --detailed-analysis --output csv

# 3. Стресс-тест лучших моделей
python stress_test_models.py --model-type rf

# 4. Отправить результаты мне на анализ
```

**Отправить**:
- `ml_models_comparison_*.csv`
- `stress_test_all_*.json`

---

## ⏱️ Временные затраты

| Действие | Время |
|----------|-------|
| Переобучение моделей | 30-60 мин |
| Сравнение результатов | 10-20 мин |
| Оптимизация гиперпараметров (RF) | 2-4 часа |
| Оптимизация гиперпараметров (XGB) | 2-4 часа |
| Оптимизация гиперпараметров (LGB) | 2-4 часа |
| Оптимизация весов ансамблей | 30-60 мин |
| Стресс-тестирование | 20-40 мин |

**Итого минимальный вариант**: ~1.5 часа
**Итого полный вариант**: ~8-12 часов (с оптимизацией)

---

## 📝 Чек-лист

- [ ] Шаг 1: Переобучение моделей
- [ ] Шаг 2: Сравнение результатов (baseline)
- [ ] Шаг 3: Сравнение с предыдущими (если есть)
- [ ] Шаг 4: Оптимизация гиперпараметров (опционально)
- [ ] Шаг 5: Переобучение с оптимальными параметрами (если делали шаг 4)
- [ ] Шаг 6: Оптимизация весов ансамблей (опционально)
- [ ] Шаг 7: Стресс-тестирование
- [ ] Шаг 8: Отправить результаты на анализ

---

## 💡 Рекомендации

1. **Сначала сделайте минимальный вариант** (шаги 1-2-3-7-8)
2. **Если результаты хорошие** - можно пропустить оптимизацию
3. **Если результаты хуже предыдущих** - обязательно сделайте оптимизацию
4. **Оптимизацию запускайте на ночь** - она занимает много времени
5. **Сохраняйте все результаты** - они понадобятся для сравнения

---

## 🎯 Ожидаемые результаты

После выполнения всех шагов вы получите:
- ✅ Лучшие модели для каждого символа
- ✅ Оптимальные гиперпараметры (если делали оптимизацию)
- ✅ Оптимальные веса ансамблей (если делали оптимизацию)
- ✅ Анализ стабильности на разных периодах
- ✅ Рекомендации по дальнейшим улучшениям
