# 🤖 ML Trading Bot

Автоматизированный торговый бот с использованием машинного обучения для торговли на бирже Bybit.

## 🚀 Быстрый старт

### Локальная установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/ml_bot.git
cd ml_bot
```

2. **Создайте виртуальное окружение:**
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cp .env.example .env  # Если есть пример
# Отредактируйте .env файл
```

5. **Запустите бота:**
```bash
python run_bot.py
```

## 📋 Требования

- Python 3.8+
- API ключи от Bybit
- Telegram Bot Token (от @BotFather)

## ⚙️ Конфигурация

Создайте файл `.env` в корне проекта:

```env
# Bybit API
BYBIT_API_KEY=your_api_key
BYBIT_API_SECRET=your_api_secret
BYBIT_BASE_URL=https://api.bybit.com

# Telegram
TELEGRAM_TOKEN=your_telegram_token
ALLOWED_USER_ID=your_user_id

# Trading
TRADING_SYMBOL=BTCUSDT
LEVERAGE=10
TIMEFRAME=15m

# ML Strategy
ML_MODEL_TYPE=quad_ensemble
ML_MTF_ENABLED=1
ML_CONFIDENCE_THRESHOLD=0.5
```

## 📚 Документация

- [Инструкция по деплою](DEPLOY.md) - подробная инструкция по деплою на сервер
- [Установка Telegram бота](INSTALL_TELEGRAM_BOT.md)

## 🎯 Основные возможности

- 🤖 **ML модели**: Random Forest, XGBoost, LightGBM, Ensemble модели (включая QuadEnsemble с LSTM)
- 📊 **Мультитаймфреймовый анализ**: Использование данных с разных таймфреймов
- 💰 **Управление рисками**: Стоп-лосс, тейк-профит, трейлинг стоп
- 📱 **Telegram интеграция**: Полный контроль через Telegram бота
- 📈 **Бэктестинг**: Тестирование стратегий на исторических данных
- 🔄 **Автоматическое переобучение**: Периодическое обновление моделей

## 📁 Структура проекта

```
ml_bot/
├── bot/                    # Основной код бота
│   ├── config.py           # Конфигурация
│   ├── trading_loop.py      # Торговый цикл
│   ├── telegram_bot.py     # Telegram бот
│   ├── ml/                 # ML компоненты
│   └── exchange/           # Интеграция с биржей
├── ml_models/              # Обученные модели
├── ml_data/                # Исторические данные
├── logs/                   # Логи
├── run_bot.py             # Точка входа
├── requirements.txt        # Зависимости
└── deploy.sh              # Скрипт деплоя
```

## 🔧 Обучение моделей

### Обучение всех моделей для символа:

```bash
python retrain_all_models.py --symbol BTCUSDT
```

### Обучение конкретного типа модели:

```bash
python retrain_ml_optimized.py --symbol BTCUSDT
python train_quad_ensemble.py --symbol BTCUSDT --days 180
# Примечание: LSTM используется только внутри quad_ensemble и не сохраняется как отдельная модель
```

## 🧪 Тестирование

### Бэктест модели:

```bash
python backtest_ml_strategy.py --model ml_models/ensemble_BTCUSDT_15_15m.pkl --symbol BTCUSDT --days 30
```

### Сравнение моделей:

```bash
python compare_ml_models.py
```

## 🚀 Деплой на сервер

См. подробную инструкцию в [DEPLOY.md](DEPLOY.md)

Быстрый деплой:

```bash
./deploy.sh
```

## 📊 Мониторинг

- Логи: `logs/bot.log`, `logs/errors.log`, `logs/trades.log`
- Статус через Telegram бота: `/status`
- Health Monitor автоматически отслеживает состояние бота

## ⚠️ Предупреждение

**Этот бот предназначен для образовательных целей. Торговля криптовалютами связана с высокими рисками. Используйте на свой страх и риск.**

## 📝 Лицензия

[Укажите вашу лицензию]

## 🤝 Вклад

Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

## 📧 Контакты

[Ваши контакты]
