# Быстрый старт: Применение оптимизированных весов

## 🚀 Шаг 1: Проверка весов

```bash
python apply_optimized_weights.py --list
```

Это покажет все доступные файлы с оптимизированными весами.

## 🎯 Шаг 2: Переобучение с оптимизированными весами

```bash
# Переобучение всех символов с оптимизированными весами
python retrain_ml_optimized.py --use-optimized-weights

# Для конкретного символа
python retrain_ml_optimized.py --symbol SOLUSDT --use-optimized-weights

# Без MTF фичей
python retrain_ml_optimized.py --no-mtf --use-optimized-weights
```

## 📊 Что происходит

1. Скрипт автоматически находит последний файл `ensemble_weights_all_*.json`
2. Извлекает веса для RF и XGB моделей
3. Применяет их при создании ансамблей
4. Сохраняет веса в метаданных модели

## ✅ Результат

После переобучения вы увидите в логах:
```
[OK] Используются оптимизированные веса: RF=1.000, XGB=0.000
[OK] Ансамбль создан с оптимизированными весами
      📊 Веса: RF=1.000, XGB=0.000 (оптимизированные)
```

## 🔍 Проверка

После переобучения протестируйте модели:
```bash
python compare_ml_models.py --detailed-analysis
```

Это покажет, улучшились ли результаты с оптимизированными весами.
