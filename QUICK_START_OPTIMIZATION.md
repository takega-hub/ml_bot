# Быстрый старт: Оптимизация лучших моделей

## 🎯 Цель
Оптимизировать гиперпараметры и веса ансамблей для лучших моделей.

## 📊 Лучшие модели для оптимизации

### RF модели:
- `rf_SOLUSDT_15_15m` (97.86% PnL)
- `rf_ADAUSDT_15_15m` (95.57% PnL)

### XGBoost модели:
- `xgb_SOLUSDT_15_15m` (88.37% PnL)

### Ensemble модели (оптимизация весов):
- `ensemble_SOLUSDT_15_15m` (109.99% PnL)
- `triple_ensemble_SOLUSDT_15_15m` (87.24% PnL)
- `ensemble_ADAUSDT_15_15m` (73.04% PnL)
- `quad_ensemble_ETHUSDT_15_15m` (74.83% PnL)
- `ensemble_BTCUSDT_15_15m` (60.34% PnL)
- `ensemble_BNBUSDT_15_15m` (72.58% PnL)

## 🚀 Варианты запуска

### Вариант 1: Автоматическая оптимизация всех моделей
```bash
python optimize_best_models.py --all
```

### Вариант 2: Оптимизация только RF моделей
```bash
python optimize_best_models.py --model-type rf
```

### Вариант 3: Оптимизация только XGBoost моделей
```bash
python optimize_best_models.py --model-type xgb
```

### Вариант 4: Оптимизация только для SOLUSDT
```bash
python optimize_best_models.py --symbol SOLUSDT
```

### Вариант 5: Оптимизация только весов ансамблей
```bash
python optimize_best_models.py --ensemble-only
```

### Вариант 6: Оптимизация только гиперпараметров
```bash
python optimize_best_models.py --hyperparameters-only
```

### Вариант 7: Использование готовых скриптов (Windows)
```bash
run_optimization.bat
```

### Вариант 8: Использование готовых скриптов (Linux/Mac)
```bash
chmod +x run_optimization.sh
./run_optimization.sh
```

## 📋 Ручная оптимизация (пошагово)

### Шаг 1: Оптимизация гиперпараметров RF для SOLUSDT
```bash
python optimize_hyperparameters.py --model rf --symbol SOLUSDT --interval 15
```

### Шаг 2: Оптимизация гиперпараметров RF для ADAUSDT
```bash
python optimize_hyperparameters.py --model rf --symbol ADAUSDT --interval 15
```

### Шаг 3: Оптимизация гиперпараметров XGBoost для SOLUSDT
```bash
python optimize_hyperparameters.py --model xgb --symbol SOLUSDT --interval 15
```

### Шаг 4: Оптимизация весов ансамбля для SOLUSDT
```bash
python optimize_ensemble_weights.py \
    --symbol SOLUSDT \
    --days 30 \
    --models "rf_SOLUSDT_15_15m.pkl,xgb_SOLUSDT_15_15m.pkl,lgb_SOLUSDT_15_15m.pkl"
```

### Шаг 5: Оптимизация весов ансамбля для ADAUSDT
```bash
python optimize_ensemble_weights.py \
    --symbol ADAUSDT \
    --days 30 \
    --models "rf_ADAUSDT_15_15m.pkl,xgb_ADAUSDT_15_15m.pkl,lgb_ADAUSDT_15_15m.pkl"
```

## ⏱️ Ожидаемое время

- **RF оптимизация**: ~30-60 минут на модель
- **XGBoost оптимизация**: ~30-60 минут на модель
- **Оптимизация весов**: ~10-20 минут на ансамбль

**Общее время для всех моделей**: ~4-6 часов

## 📁 Результаты

Результаты сохраняются в JSON файлы:
- `hyperparameters_optimization_YYYYMMDD.json` - результаты оптимизации гиперпараметров
- `ensemble_weights_SYMBOL_YYYYMMDD.json` - результаты оптимизации весов
- `optimization_results_YYYYMMDD_HHMMSS.json` - сводка всех результатов

## 🔄 Следующие шаги после оптимизации

1. **Проверьте результаты** в JSON файлах
2. **Примените оптимизированные параметры** к `retrain_ml_optimized.py`
3. **Переобучите модели** с новыми параметрами
4. **Протестируйте оптимизированные модели** через `compare_ml_models.py`
5. **Сравните результаты** с исходными моделями

## ⚠️ Примечания

- Grid Search может занять много времени (30-60 минут на модель)
- Убедитесь, что у вас достаточно данных для оптимизации (минимум 2000 свечей)
- Результаты оптимизации зависят от периода данных - используйте последние данные
- После оптимизации обязательно перетестируйте модели на новых данных
