# 🚀 БЫСТРЫЙ СТАРТ: ОПТИМИЗИРОВАННАЯ ОПТИМИЗАЦИЯ СТРАТЕГИЙ

## 📋 ЧТО ИЗМЕНИЛОСЬ

Процесс оптимизации теперь **автоматически выбирает топ-модели** и тестирует только их комбинации, что ускоряет процесс в **4-5 раз**.

## ⚡ БЫСТРЫЙ ЗАПУСК

### Для всех активных символов (из runtime_state.json):

```bash
python auto_strategy_optimizer.py
```

### Для конкретных символов:

```bash
python auto_strategy_optimizer.py --symbols BTCUSDT,ETHUSDT,SOLUSDT
```

### Без обучения (если модели уже есть):

```bash
python auto_strategy_optimizer.py --skip-training
```

## 📊 ЧТО ПРОИСХОДИТ

1. **Обучение моделей** (если не пропущено)
   - 15m модели для каждого символа
   - 1h модели для каждого символа

2. **Сравнение моделей**
   - Тестирует все одиночные модели (15m и 1h)
   - Сохраняет результаты в `ml_models_comparison_*.csv`
   - Вычисляет composite score

3. **Оптимизированное тестирование MTF** ⚡
   - Выбирает топ-5 моделей каждого таймфрейма
   - Тестирует только 25 комбинаций (5×5) вместо всех
   - Сохраняет результаты в `mtf_combinations_*_*.csv`

4. **Выбор лучших стратегий**
   - Анализирует результаты
   - Выбирает лучшую стратегию для каждого символа
   - Сохраняет в `best_strategies_*.json`

## ⏱️ ВРЕМЯ ВЫПОЛНЕНИЯ

- **Обучение моделей:** ~30-60 минут на символ
- **Сравнение моделей:** ~1-2 часа для всех символов
- **MTF тестирование (оптимизированное):** ~4-6 часов (вместо 16-25 часов)
- **Итого:** ~6-9 часов (вместо 20-30 часов)

## 🎛️ ДОПОЛНИТЕЛЬНЫЕ ОПЦИИ

### Изменить количество топ-моделей:

```bash
# Тестировать топ-3×3 = 9 комбинаций (быстрее)
python auto_strategy_optimizer.py --mtf-top-n 3

# Тестировать топ-7×7 = 49 комбинаций (точнее)
python auto_strategy_optimizer.py --mtf-top-n 7
```

### Полное тестирование всех комбинаций:

```bash
python auto_strategy_optimizer.py --full-mtf-testing
```

### Пропуск отдельных этапов:

```bash
# Только MTF тестирование (если сравнение уже выполнено)
python auto_strategy_optimizer.py --skip-training --skip-comparison

# Только сравнение моделей
python auto_strategy_optimizer.py --skip-training --skip-mtf-testing
```

## 📁 РЕЗУЛЬТАТЫ

После выполнения в директории `optimization_results/` будут:

- `mtf_combinations_*_*.csv` - результаты MTF комбинаций для каждого символа
- `best_strategies_*.json` - лучшие стратегии для каждого символа
- `optimization_*.log` - подробный лог процесса

## 💡 РЕКОМЕНДАЦИИ

1. **Регулярная оптимизация:** используйте оптимизированный подход (по умолчанию)
2. **Еженедельно:** полный цикл с обучением
3. **Ежедневно:** быстрая оптимизация без обучения (`--skip-training`)
4. **Раз в месяц:** полное тестирование всех комбинаций (`--full-mtf-testing`)

## 🔍 ПРОВЕРКА РЕЗУЛЬТАТОВ

После выполнения проверьте:

1. **Файл сравнения:** `ml_models_comparison_*.csv` - должны быть и 15m, и 1h модели
2. **MTF результаты:** `mtf_combinations_*_*.csv` - должны быть результаты для каждого символа
3. **Лучшие стратегии:** `best_strategies_*.json` - финальный выбор стратегий

## ❓ ВОПРОСЫ

**Q: Почему тестируется только 25 комбинаций?**
A: Анализ показал, что лучшие MTF комбинации используют модели из топ-5 одиночных. Это ускоряет процесс в 4-5 раз без потери качества.

**Q: Можно ли протестировать все комбинации?**
A: Да, используйте флаг `--full-mtf-testing`

**Q: Как изменить количество топ-моделей?**
A: Используйте параметр `--mtf-top-n N` (по умолчанию 5)

---

*Обновлено: 2025-02-14*
